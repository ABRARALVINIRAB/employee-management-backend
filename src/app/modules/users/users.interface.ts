export interface IUser {
    name: string;
    email: string;
    gender: string;
    phoneNumber: string;
    employeeId: string;
    
    isApproved: boolean;
    // Add any other user-related fields here
  }